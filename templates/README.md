# Template Packages

Every subdirectory is one downloadable `fallback-html` template package.

Required files:

- `manifest.json`
- at least `pages/index.html`
- optional static files under `assets/`

Keep the files static and boring: HTML/CSS only, no active browser code and no panel-specific identifiers.
